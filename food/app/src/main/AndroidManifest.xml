<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fooddeliveryclient">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hello"
        android:label="Home Sweet Home"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".List_Office"></activity>
        <activity android:name=".Chooser" />
        <activity android:name=".WorkPlaceFood" />
        <activity android:name=".List"/>
        <activity android:name=".Detailfood" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>